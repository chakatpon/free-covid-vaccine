import React, { ReactElement } from "react";
import Vaccine from "../../components/vaccine/Vaccine";

const VaccineView: React.FC = (): ReactElement => {
  return <Vaccine />;
};

export default VaccineView;
